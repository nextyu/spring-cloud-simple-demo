# 配置服务注册中心
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://172.168.2.69:8761/eureka/

spring:
  application:
    name: order-service
  cloud:
    config:
      uri: http://172.18.2.69:8888/
      name: cloud-config
      profile: dev

